<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Submissions - Admin</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: #181828;
            color: #fff;
            min-height: 100vh;
            padding: 40px 20px;
        }

        .admin-container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 32px;
            flex-wrap: wrap;
            gap: 16px;
        }

        h1 {
            font-size: 2.2rem;
            font-weight: bold;
            background: linear-gradient(90deg, #ff7ee7, #6a82fb);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .header-actions {
            display: flex;
            gap: 12px;
            align-items: center;
        }

        .logout-btn, .team-photos-btn {
            padding: 10px 20px;
            border-radius: 8px;
            background: #181828;
            border: 1.5px solid #2d2d44;
            color: #fff;
            text-decoration: none;
            font-size: 0.95rem;
            transition: background 0.2s;
        }

        .team-photos-btn {
            background: linear-gradient(90deg, #6a82fb, #ff7ee7);
            border: none;
        }

        .logout-btn:hover, .team-photos-btn:hover {
            background: #2d2d44;
        }

        .team-photos-btn:hover {
            background: linear-gradient(90deg, #5a72eb, #ef6ed7);
        }

        .stats {
            display: flex;
            gap: 20px;
            margin-bottom: 32px;
            flex-wrap: wrap;
        }

        .stat-card {
            background: #23233a;
            border-radius: 12px;
            padding: 20px 24px;
            flex: 1;
            min-width: 200px;
            box-shadow: 0 4px 20px rgba(106,130,251,0.1);
        }

        .stat-label {
            color: #b6b6d6;
            font-size: 0.9rem;
            margin-bottom: 8px;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: bold;
            background: linear-gradient(90deg, #ff7ee7, #6a82fb);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .table-container {
            background: #23233a;
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 4px 32px rgba(106,130,251,0.1);
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th {
            text-align: left;
            padding: 12px 16px;
            color: #b6b6d6;
            font-weight: 600;
            font-size: 0.9rem;
            border-bottom: 1px solid #2d2d44;
            white-space: nowrap;
        }

        td {
            padding: 16px;
            border-bottom: 1px solid #2d2d44;
            color: #d1d1e0;
        }

        tr:last-child td {
            border-bottom: none;
        }

        tr:hover {
            background: rgba(106,130,251,0.05);
        }

        .view-link {
            color: #6a82fb;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.2s;
        }

        .view-link:hover {
            color: #ff7ee7;
        }

        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 6px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .badge-package {
            background: linear-gradient(90deg, rgba(255,126,231,0.15), rgba(106,130,251,0.15));
            border: 1px solid rgba(106,130,251,0.3);
            color: #a5b4fc;
        }

        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #b6b6d6;
        }

        .empty-state-icon {
            font-size: 3rem;
            margin-bottom: 16px;
            opacity: 0.5;
        }

        .btn-delete {
            padding: 6px 12px;
            border-radius: 6px;
            background: rgba(220, 38, 38, 0.1);
            border: 1px solid rgba(220, 38, 38, 0.3);
            color: #ff6b6b;
            text-decoration: none;
            font-size: 0.85rem;
            font-weight: 500;
            transition: all 0.2s;
            cursor: pointer;
            margin-left: 8px;
        }

        .btn-delete:hover {
            background: rgba(220, 38, 38, 0.2);
            border-color: rgba(220, 38, 38, 0.5);
        }

        .action-buttons {
            display: flex;
            align-items: center;
            gap: 8px;
            flex-wrap: wrap;
        }

        @keyframes fadeOut {
            from {
                opacity: 1;
                transform: translateX(0);
            }
            to {
                opacity: 0;
                transform: translateX(-20px);
            }
        }

        tr.deleting {
            animation: fadeOut 0.3s ease-out forwards;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 1.8rem;
            }

            .table-container {
                padding: 16px;
            }

            table {
                font-size: 0.9rem;
            }

            th, td {
                padding: 10px 12px;
            }
        }
    </style>
</head>
<body>
    <div class="admin-container">
        <div class="header">
            <h1>All Submissions</h1>
            <div style="display: flex; gap: 12px; flex-wrap: wrap;">
                <a href="/admin/audits" class="logout-btn">Audits</a>
                <a href="/admin/preaudits" class="logout-btn">Preaudits</a>
                <a href="/admin/team-photos" class="team-photos-btn">👥 Team Photos</a>
                <a href="/admin/logout" class="logout-btn">Logout</a>
            </div>
        </div>

        <div class="stats">
            <div class="stat-card">
                <div class="stat-label">Contact Form Submissions</div>
                <div class="stat-value"><%= (contactSubmissions || []).length %></div>
            </div>
            <div class="stat-card">
                <div class="stat-label">Web Project Form Submissions</div>
                <div class="stat-value"><%= (webProjectSubmissions || []).length %></div>
            </div>
            <div class="stat-card">
                <div class="stat-label">Audit Jobs</div>
                <div class="stat-value"><%= (auditJobs || []).length %></div>
            </div>
            <div class="stat-card">
                <div class="stat-label">Total Submissions</div>
                <div class="stat-value"><%= (contactSubmissions || []).length + (webProjectSubmissions || []).length %></div>
            </div>
        </div>

        <!-- Contact Form Submissions -->
        <h2 style="margin: 32px 0 16px 0; font-size: 1.5rem; color: #d1d1e0;">Contact Form Submissions</h2>
        <div class="table-container" style="margin-bottom: 32px;">
            <% if (!contactSubmissions || contactSubmissions.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-state-icon">📭</div>
                    <p>No contact form submissions yet</p>
                </div>
            <% } else { %>
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Date</th>
                            <th>Email</th>
                            <th>Name</th>
                            <th>Company</th>
                            <th>Budget</th>
                            <th>Industry</th>
                            <th>Package</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% contactSubmissions.forEach(submission => { %>
                            <tr id="contact-row-<%= submission.id %>">
                                <td>#<%= submission.id %></td>
                                <td><%= new Date(submission.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                                <td><%= submission.email %></td>
                                <td><%= submission.name || '-' %></td>
                                <td><%= submission.company || '-' %></td>
                                <td><%= submission.budget_range || '-' %></td>
                                <td><%= submission.industry || '-' %></td>
                                <td>
                                    <% if (submission.selected_package) { %>
                                        <span class="badge badge-package"><%= submission.selected_package %></span>
                                    <% } else { %>
                                        -
                                    <% } %>
                                </td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="/admin/contact/<%= submission.id %>" class="view-link">View →</a>
                                        <button class="btn-delete btn-delete-contact" data-id="<%= submission.id %>" data-email="<%= submission.email || '' %>">Delete</button>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } %>
        </div>

        <!-- Web Project Form Submissions -->
        <h2 style="margin: 32px 0 16px 0; font-size: 1.5rem; color: #d1d1e0;">Web Project Form Submissions</h2>
        <div class="table-container">
            <% if (!webProjectSubmissions || webProjectSubmissions.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-state-icon">📭</div>
                    <p>No web project form submissions yet</p>
                </div>
            <% } else { %>
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Date</th>
                            <th>Company Name</th>
                            <th>Website</th>
                            <th>Deadline</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% webProjectSubmissions.forEach(submission => { %>
                            <tr id="web-project-row-<%= submission.id %>">
                                <td>#<%= submission.id %></td>
                                <td><%= new Date(submission.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                                <td><%= submission.company_name || '-' %></td>
                                <td><%= submission.current_website || '-' %></td>
                                <td><%= submission.deadline || '-' %></td>
                                <td>
                                    <div class="action-buttons">
                                        <a href="/admin/web-project/<%= submission.id %>" class="view-link">View →</a>
                                        <button class="btn-delete btn-delete-web-project" data-id="<%= submission.id %>" data-company="<%= submission.company_name || '' %>">Delete</button>
                                    </div>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } %>
        </div>

        <!-- Audit Jobs -->
        <h2 style="margin: 32px 0 16px 0; font-size: 1.5rem; color: #d1d1e0;">Audit Jobs</h2>
        <div class="table-container">
            <% if (!auditJobs || auditJobs.length === 0) { %>
                <div class="empty-state">
                    <div class="empty-state-icon">🧩</div>
                    <p>No audit jobs yet</p>
                </div>
            <% } else { %>
                <table>
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Date</th>
                            <th>Niche</th>
                            <th>URL</th>
                            <th>Status</th>
                            <th>Public URL</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% auditJobs.forEach(job => { %>
                            <tr>
                                <td>#<%= job.id %></td>
                                <td><%= new Date(job.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                                <td><%= job.niche %></td>
                                <td><%= job.input_url %></td>
                                <td><span class="badge badge-package"><%= job.status %></span></td>
                                <td>
                                    <% if (job.public_page_slug) { %>
                                        <a href="/<%= job.public_page_slug %>" class="view-link" target="_blank">Open →</a>
                                    <% } else { %>
                                        -
                                    <% } %>
                                </td>
                                <td>
                                    <a href="/admin/audits/<%= job.id %>" class="view-link">View →</a>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } %>
        </div>
    </div>

    <script>
        // Attach event listeners to all delete buttons
        document.addEventListener('DOMContentLoaded', function() {
            // Contact submissions delete
            const deleteContactButtons = document.querySelectorAll('.btn-delete-contact');
            deleteContactButtons.forEach(button => {
                button.addEventListener('click', async function() {
                    const id = this.getAttribute('data-id');
                    const email = this.getAttribute('data-email');
                    
                    // First confirmation
                    const emailDisplay = email ? `\n\nEmail: ${email}` : '';
                    const confirmFirst = confirm(`Are you sure you want to delete contact submission #${id}?${emailDisplay}`);
                    
                    if (!confirmFirst) {
                        return;
                    }
                    
                    // Second confirmation
                    const confirmSecond = confirm(`This will permanently delete contact submission #${id}.\n\nThis action cannot be undone. Continue?`);
                    
                    if (!confirmSecond) {
                        return;
                    }
                    
                    // Show deleting state
                    const row = document.getElementById('contact-row-' + id);
                    if (row) {
                        row.classList.add('deleting');
                    }
                    
                    try {
                        const response = await fetch('/admin/api/contact-submissions/' + id, {
                            method: 'DELETE',
                            headers: {
                                'Accept': 'application/json'
                            },
                            credentials: 'same-origin'
                        });
                        
                        if (response.status === 401) {
                            alert('Session expired. Please log in again.');
                            window.location.href = '/admin/login';
                            return;
                        }
                        
                        const data = await response.json();
                        
                        if (!response.ok) {
                            throw new Error(data.message || 'Failed to delete contact submission');
                        }
                        
                        // Successfully deleted - remove row after animation
                        if (row) {
                            setTimeout(() => {
                                row.remove();
                                
                                // Check if table is now empty
                                const tbody = row.closest('tbody');
                                if (tbody && tbody.children.length === 0) {
                                    window.location.reload();
                                }
                            }, 300);
                        }
                        
                    } catch (error) {
                        console.error('Error deleting contact submission:', error);
                        alert('Failed to delete contact submission: ' + error.message);
                        
                        if (row) {
                            row.classList.remove('deleting');
                        }
                    }
                });
            });

            // Web project submissions delete
            const deleteWebProjectButtons = document.querySelectorAll('.btn-delete-web-project');
            deleteWebProjectButtons.forEach(button => {
                button.addEventListener('click', async function() {
                    const id = this.getAttribute('data-id');
                    const company = this.getAttribute('data-company');
                    
                    // First confirmation
                    const companyDisplay = company ? `\n\nCompany: ${company}` : '';
                    const confirmFirst = confirm(`Are you sure you want to delete web project submission #${id}?${companyDisplay}`);
                    
                    if (!confirmFirst) {
                        return;
                    }
                    
                    // Second confirmation
                    const confirmSecond = confirm(`This will permanently delete web project submission #${id}.\n\nThis action cannot be undone. Continue?`);
                    
                    if (!confirmSecond) {
                        return;
                    }
                    
                    // Show deleting state
                    const row = document.getElementById('web-project-row-' + id);
                    if (row) {
                        row.classList.add('deleting');
                    }
                    
                    try {
                        const response = await fetch('/admin/api/web-project-submissions/' + id, {
                            method: 'DELETE',
                            headers: {
                                'Accept': 'application/json'
                            },
                            credentials: 'same-origin'
                        });
                        
                        if (response.status === 401) {
                            alert('Session expired. Please log in again.');
                            window.location.href = '/admin/login';
                            return;
                        }
                        
                        const data = await response.json();
                        
                        if (!response.ok) {
                            throw new Error(data.message || 'Failed to delete web project submission');
                        }
                        
                        // Successfully deleted - remove row after animation
                        if (row) {
                            setTimeout(() => {
                                row.remove();
                                
                                // Check if table is now empty
                                const tbody = row.closest('tbody');
                                if (tbody && tbody.children.length === 0) {
                                    window.location.reload();
                                }
                            }, 300);
                        }
                        
                    } catch (error) {
                        console.error('Error deleting web project submission:', error);
                        alert('Failed to delete web project submission: ' + error.message);
                        
                        if (row) {
                            row.classList.remove('deleting');
                        }
                    }
                });
            });
        });
    </script>
</body>
</html>


