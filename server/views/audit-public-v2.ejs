<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Website Audit: <%= job.input_url %> | Max & Jacob</title>
    <meta name="description" content="Personal website diagnosis for <%= job.niche %> in <%= job.city %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
        
        @keyframes bounce-slow {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
        
        @keyframes pulse-slow {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        .animate-bounce-slow {
            animation: bounce-slow 4s ease-in-out infinite;
        }
        
        .animate-pulse-slow {
            animation: pulse-slow 3s ease-in-out infinite;
        }
        
        .cursor-zoom-in {
            cursor: zoom-in;
        }
        
        .cursor-zoom-out {
            cursor: zoom-out;
        }
    </style>
</head>
<body class="bg-[#fcfdfe] text-slate-900 antialiased">
    
    <!-- HEADER -->
    <header class="fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-2xl border-b border-slate-100">
        <div class="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
            <div class="font-black text-2xl tracking-tighter text-blue-600">
                Max & Jacob
            </div>
            <a href="#form" class="bg-blue-600 text-white px-6 py-2.5 rounded-2xl text-sm font-black hover:bg-blue-700 shadow-xl shadow-blue-100 transition-all hover:scale-105 active:scale-95">
                Get the 7-Day Plan
            </a>
        </div>
    </header>

    <!-- Zoom Modal -->
    <div id="zoomModal" class="hidden fixed inset-0 z-[100] bg-slate-900/95 backdrop-blur-md flex items-center justify-center p-6 cursor-zoom-out">
        <img id="zoomImage" src="" class="max-w-full max-h-[90vh] rounded-[2rem] shadow-2xl border-4 border-white/10">
    </div>

    <!-- HERO SECTION -->
    <section class="pt-32 lg:pt-40 pb-24 px-6 max-w-[90rem] mx-auto overflow-visible">
        <div class="grid lg:grid-cols-12 gap-16 lg:gap-32 items-center">
            <div class="lg:col-span-5 text-center lg:text-left order-2 lg:order-1 relative z-40 lg:pl-8">
                <div class="inline-block px-4 py-1.5 bg-red-50 text-red-600 rounded-full text-[10px] font-black uppercase tracking-[0.2em] mb-8 border border-red-200 animate-pulse">
                    ⚠️ <%= vm.top_3_issues.length %> Critical Issues Found
                </div>
                <h1 class="text-5xl md:text-8xl font-black text-slate-900 tracking-tighter leading-[0.9] mb-10">
                    <%= vm.hero.headline %>
                </h1>
                <p class="text-xl text-slate-500 font-medium leading-relaxed mb-12 max-w-xl mx-auto lg:mx-0">
                    <%= vm.hero.subheadline %>
                </p>
                
                <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
                    <a href="#fixes" class="px-12 py-6 bg-blue-600 text-white rounded-[2rem] font-black text-2xl hover:bg-blue-700 shadow-2xl shadow-blue-200 transition-all transform hover:-translate-y-2 active:scale-95">
                        <%= vm.hero.primary_cta_text %>
                    </a>
                </div>
            </div>
            
            <div class="lg:col-span-7 order-1 lg:order-2 flex justify-center lg:-mt-24">
                <!-- Hero Visuals -->
                <div class="relative w-full h-[500px] lg:h-[700px] flex items-center justify-center">
                    <!-- Large Desktop Mockup -->
                    <% if (vm.aha_moment.has_screenshot) { %>
                    <div class="relative z-10 w-full max-w-[1050px]">
                        <div class="bg-slate-900 rounded-t-2xl p-3 flex gap-2 items-center">
                            <div class="w-3 h-3 rounded-full bg-red-500"></div>
                            <div class="w-3 h-3 rounded-full bg-amber-500"></div>
                            <div class="w-3 h-3 rounded-full bg-emerald-500"></div>
                            <div class="mx-auto bg-white/10 rounded-lg px-4 py-1 text-[10px] text-white/40 font-mono tracking-widest uppercase">Analyzing Presence</div>
                        </div>
                        <div class="bg-white p-1 rounded-b-3xl shadow-[0_60px_120px_-30px_rgba(0,0,0,0.3)] border-x border-b border-slate-200 overflow-hidden aspect-[16/10] cursor-zoom-in" onclick="openZoom('<%= vm.aha_moment.screenshot_url %>')">
                            <img src="<%= vm.aha_moment.screenshot_url %>" alt="Desktop Preview" class="w-full object-top">
                        </div>

                        <!-- Low Clarity Tag -->
                        <div class="absolute -top-8 -left-4 md:-left-16 bg-red-600 text-white p-7 rounded-[2.5rem] shadow-2xl animate-bounce-slow z-30">
                            <p class="text-[10px] font-black uppercase tracking-[0.2em] opacity-80 mb-1">Diagnosis</p>
                            <p class="text-3xl font-black tracking-tighter">CRITICAL FRICTION</p>
                        </div>
                    </div>

                    <!-- Refined Mobile Mockup (from 7-day-web-clarity-plan) -->
                    <% if (vm.aha_moment.mobile_screenshot_url) { %>
                    <div class="absolute -bottom-8 -right-2 lg:-right-4 z-20 w-[160px] md:w-[260px]">
                        <div class="bg-slate-900 p-1.5 md:p-2 rounded-[3rem] shadow-2xl border border-slate-700/50 ring-1 ring-slate-200/50">
                            <div class="bg-white rounded-[2.25rem] overflow-hidden aspect-[9/19] cursor-zoom-in" onclick="openZoom('<%= vm.aha_moment.mobile_screenshot_url %>')">
                                <img src="<%= vm.aha_moment.mobile_screenshot_url %>" alt="Mobile Preview" class="w-full object-top">
                            </div>
                        </div>
                    </div>
                    <% } %>
                    <% } else { %>
                    <div class="h-full bg-slate-50 flex items-center justify-center text-slate-200 font-black text-4xl uppercase tracking-tighter rounded-3xl border-2 border-dashed border-slate-200 p-12">
                        Audit Preview
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </section>

    <!-- GROWTH GAP SECTION -->
    <section class="py-24 px-6 bg-white border-y border-slate-100">
        <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-20 items-center">
            <!-- Growth Chart -->
            <div class="bg-white p-10 rounded-[3rem] border border-slate-100 shadow-2xl relative overflow-hidden group">
                <div class="absolute top-0 right-0 p-4">
                    <div class="px-3 py-1 bg-emerald-100 text-emerald-600 rounded-full text-[10px] font-black uppercase">Growth Gap</div>
                </div>
                <div class="space-y-10">
                    <div class="space-y-2">
                        <p class="text-[10px] font-black text-slate-400 uppercase tracking-[0.2em]">Current Business Impact</p>
                        <div class="h-4 bg-slate-100 rounded-full w-full overflow-hidden">
                            <div class="h-full bg-slate-300" style="width: <%= vm.growth_chart.current_impact %>%"></div>
                        </div>
                    </div>
                    <div class="space-y-2">
                        <p class="text-[10px] font-black text-blue-600 uppercase tracking-[0.2em]">New Clarity Potential</p>
                        <div class="h-8 bg-blue-50 rounded-full w-full overflow-hidden relative">
                            <div class="h-full bg-blue-600 animate-pulse-slow" style="width: <%= vm.growth_chart.potential_impact %>%"></div>
                            <div class="absolute inset-0 flex items-center px-4">
                                <span class="text-white text-[10px] font-black">UNLOCKED PERFORMANCE</span>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="mt-10 flex items-center gap-6">
                    <div class="w-16 h-16 bg-emerald-500 text-white rounded-[1.5rem] flex items-center justify-center shadow-xl shadow-emerald-200">
                        <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" /></svg>
                    </div>
                    <div>
                        <p class="text-3xl font-black text-slate-900 tracking-tight">+<%= vm.growth_chart.estimated_lift %>%</p>
                        <p class="text-xs font-bold text-slate-500">Estimated Inquiry Lift</p>
                    </div>
                </div>
            </div>
            
            <div class="space-y-10">
                <h2 class="text-4xl md:text-6xl font-black text-slate-900 tracking-tight leading-tight">Every day without these fixes costs you real customers.</h2>
                <p class="text-xl text-slate-500 font-medium leading-relaxed">
                    Right now, <%= vm.hero.brand_or_domain %> is missing <%= vm.growth_chart.estimated_lift %>% of potential inquiries. 
                    <span class="text-blue-600 font-black">We'll show you exactly how to fix it in 7 days—no endless revisions, no surprises.</span>
                </p>
                <div class="flex flex-wrap gap-8">
                    <div class="flex items-center gap-3">
                        <span class="text-3xl font-black text-slate-900"><%= vm.growth_chart.stats.capture_time %></span>
                        <p class="text-[10px] font-black text-slate-400 uppercase leading-tight">To capture<br/>attention</p>
                    </div>
                    <div class="flex items-center gap-3">
                        <span class="text-3xl font-black text-slate-900"><%= vm.growth_chart.stats.wont_return %></span>
                        <p class="text-[10px] font-black text-slate-400 uppercase leading-tight">Wont return<br/>after a bad visit</p>
                    </div>
                    <% if (vm.seo_local.seo_score > 0) { %>
                    <div class="flex items-center gap-3">
                        <span class="text-3xl font-black <%= vm.seo_local.seo_score >= 70 ? 'text-emerald-600' : vm.seo_local.seo_score >= 40 ? 'text-amber-600' : 'text-red-600' %>"><%= vm.seo_local.seo_score %>/100</span>
                        <p class="text-[10px] font-black text-slate-400 uppercase leading-tight">Local SEO<br/>Score</p>
                    </div>
                    <% } %>
                </div>
            </div>
        </div>
    </section>

    <!-- SEO & LOCAL SEARCH VISIBILITY -->
    <section class="py-24 px-6 bg-gradient-to-br from-red-50 to-orange-50 border-y border-red-100">
        <div class="max-w-7xl mx-auto">
            <div class="text-center mb-16">
                <div class="inline-block px-4 py-2 bg-red-600 text-white rounded-full text-xs font-black uppercase tracking-wider mb-6">
                    🚨 Critical: Search & AI Visibility
                </div>
                <h2 class="text-4xl md:text-6xl font-black text-slate-900 tracking-tight mb-6">You're Invisible to <%= vm.hero.city %> Customers Everywhere.</h2>
                <p class="text-xl text-slate-600 font-medium max-w-3xl mx-auto mb-8">
                    Whether they search on Google, ask ChatGPT, or use Perplexity—<%= vm.hero.brand_or_domain %> doesn't appear. Here's the damage:
                </p>
                
                <!-- AI Search Warning -->
                <div class="bg-slate-900 text-white p-8 rounded-3xl shadow-2xl max-w-4xl mx-auto mb-12">
                    <div class="flex items-center justify-center gap-6 mb-6 flex-wrap">
                        <div class="flex items-center gap-3 opacity-50">
                            <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center">
                                <span class="text-2xl">🤖</span>
                            </div>
                            <span class="font-black text-lg">ChatGPT</span>
                        </div>
                        <div class="flex items-center gap-3 opacity-50">
                            <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center">
                                <span class="text-2xl">💎</span>
                            </div>
                            <span class="font-black text-lg">Gemini</span>
                        </div>
                        <div class="flex items-center gap-3 opacity-50">
                            <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center">
                                <span class="text-2xl">🔮</span>
                            </div>
                            <span class="font-black text-lg">Perplexity</span>
                        </div>
                        <div class="flex items-center gap-3 opacity-50">
                            <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center">
                                <span class="text-2xl">🔍</span>
                            </div>
                            <span class="font-black text-lg">Google</span>
                        </div>
                    </div>
                    <div class="text-center">
                        <p class="text-3xl font-black mb-3">❌ "<%= vm.hero.brand_or_domain %> not found"</p>
                        <p class="text-lg text-slate-300 font-medium">When AI recommends the best <%= vm.hero.niche %> in <%= vm.hero.city %>, you're not on the list.</p>
                    </div>
                </div>
            </div>

            <% if (vm.seo_local.has_issues) { %>
            <div class="grid md:grid-cols-2 gap-8 mb-16">
                <% vm.seo_local.critical_issues.forEach((item, idx) => { %>
                <div class="bg-white p-8 rounded-3xl border-2 <%= item.category === 'ai_visibility' ? 'border-purple-400 ring-4 ring-purple-100' : 'border-red-200' %> shadow-lg hover:shadow-2xl transition-all <%= item.category === 'ai_visibility' ? 'md:col-span-2' : '' %>">
                    <% if (item.category === 'ai_visibility') { %>
                    <div class="inline-block px-3 py-1 bg-purple-600 text-white rounded-full text-xs font-black uppercase tracking-wider mb-4">
                        🤖 NEW: AI Search Era
                    </div>
                    <% } %>
                    <div class="flex items-start gap-4 mb-4">
                        <div class="w-12 h-12 <%= item.category === 'ai_visibility' ? 'bg-purple-600' : 'bg-red-600' %> text-white rounded-2xl flex items-center justify-center flex-shrink-0 font-black text-xl">
                            <%= idx + 1 %>
                        </div>
                        <div class="flex-1">
                            <h3 class="text-xl font-black text-slate-900 mb-2"><%= item.issue %></h3>
                            <p class="<%= item.category === 'ai_visibility' ? 'text-purple-600' : 'text-red-600' %> font-bold mb-3">💥 Impact: <%= item.impact %></p>
                            <p class="text-emerald-600 font-semibold">✅ Fix: <%= item.fix %></p>
                        </div>
                    </div>
                </div>
                <% }); %>
            </div>
            <% } %>

            <% if (vm.seo_local.opportunities && vm.seo_local.opportunities.length > 0) { %>
            <div class="bg-gradient-to-br from-emerald-600 to-blue-600 text-white p-10 rounded-[3rem] shadow-2xl">
                <h3 class="text-3xl font-black mb-8 text-center">🎯 Visibility Opportunities for <%= vm.hero.brand_or_domain %></h3>
                <div class="grid md:grid-cols-3 gap-6">
                    <% vm.seo_local.opportunities.forEach(opp => { %>
                    <div class="<%= opp.category === 'ai_visibility' ? 'bg-purple-500/30 ring-2 ring-purple-300' : 'bg-white/10' %> backdrop-blur-sm p-6 rounded-2xl border border-white/20">
                        <% if (opp.category === 'ai_visibility') { %>
                        <p class="text-xs font-black text-purple-200 mb-2">🤖 FUTURE-PROOF</p>
                        <% } else { %>
                        <p class="text-sm font-bold text-emerald-100 mb-2">OPPORTUNITY</p>
                        <% } %>
                        <h4 class="text-lg font-black mb-3"><%= opp.opportunity %></h4>
                        <p class="text-white font-semibold mb-3"><%= opp.benefit %></p>
                        <p class="text-sm text-white/80"><%= opp.action %></p>
                    </div>
                    <% }); %>
                </div>
            </div>
            <% } %>
        </div>
    </section>

    <!-- MINI AUDIT BULLETS -->
    <section class="py-24 px-6 max-w-6xl mx-auto">
        <div class="text-center mb-20">
            <h2 class="text-4xl md:text-5xl font-black tracking-tight mb-4">What We Found on <%= vm.hero.brand_or_domain %></h2>
            <p class="text-slate-500 font-medium text-lg">No sugar-coating. Here's what we discovered in our manual analysis of your <%= vm.hero.niche %> website.</p>
        </div>
        <div class="grid md:grid-cols-2 gap-12 md:gap-20">
            <div class="space-y-10">
                <div class="flex items-center gap-4">
                    <div class="w-14 h-14 bg-emerald-100 text-emerald-600 rounded-2xl flex items-center justify-center">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" /></svg>
                    </div>
                    <h3 class="text-2xl font-black text-slate-900 tracking-tight">Existing Assets</h3>
                </div>
                <ul class="space-y-4">
                    <% vm.mini_audit.well.forEach(bullet => { %>
                    <li class="flex gap-4 p-8 bg-white rounded-[2rem] border border-slate-100 text-slate-700 font-bold shadow-sm">
                        <span class="text-emerald-500">✓</span> <%= bullet.text %>
                    </li>
                    <% }); %>
                </ul>
            </div>
            <div class="space-y-10">
                <div class="flex items-center gap-4">
                    <div class="w-14 h-14 bg-red-100 text-red-600 rounded-2xl flex items-center justify-center font-black">!</div>
                    <h3 class="text-2xl font-black text-slate-900 tracking-tight">The Friction Killers</h3>
                </div>
                <ul class="space-y-4">
                    <% vm.mini_audit.unclear.forEach(bullet => { %>
                    <li class="flex gap-4 p-8 bg-red-50/30 rounded-[2rem] border border-red-100 text-slate-600 italic font-medium">
                        <span class="text-red-500 font-black">•</span> <%= bullet.text %>
                    </li>
                    <% }); %>
                </ul>
            </div>
        </div>
    </section>

    <!-- MEET THE BROTHERS -->
    <section class="px-6">
        <div class="max-w-7xl mx-auto">
            <div class="bg-blue-600 rounded-[4rem] p-12 text-white relative overflow-hidden my-20">
                <div class="max-w-6xl mx-auto flex flex-col lg:flex-row items-center gap-16 relative z-10">
                    <div class="flex flex-row gap-6">
                        <div class="text-center group">
                            <div class="w-36 h-36 md:w-56 md:h-56 rounded-3xl bg-slate-100 border-4 border-white overflow-hidden shadow-2xl mb-4 transition-transform group-hover:scale-[1.02]">
                                <img src="<%= vm.team_photos.max %>" alt="Max" class="w-full h-full object-cover">
                            </div>
                            <p class="font-black text-xl mb-0.5">Max</p>
                            <p class="text-[10px] font-black uppercase text-blue-200 tracking-widest">Co-Founder & Strategy</p>
                        </div>
                        <div class="text-center group">
                            <div class="w-36 h-36 md:w-56 md:h-56 rounded-3xl bg-slate-100 border-4 border-white overflow-hidden shadow-2xl mb-4 transition-transform group-hover:scale-[1.02]">
                                <img src="<%= vm.team_photos.jacob %>" alt="Jacob" class="w-full h-full object-cover">
                            </div>
                            <p class="font-black text-xl mb-0.5">Jacob</p>
                            <p class="text-[10px] font-black uppercase text-blue-200 tracking-widest">Co-Founder & Design</p>
                        </div>
                    </div>
                    <div class="flex-1 text-center lg:text-left">
                        <h3 class="text-4xl md:text-5xl font-black mb-6 tracking-tight">Real help from the founders.</h3>
                        <p class="text-lg md:text-xl text-blue-50 font-medium mb-10 leading-relaxed max-w-2xl">
                            "We personally analyzed <%= vm.hero.brand_or_domain %> and found <%= vm.top_3_issues.length %> critical issues holding you back. No agencies, no middlemen—just Max and Jacob working directly with you to transform your <%= vm.hero.niche %> website into a lead-generating machine."
                        </p>
                        <div class="inline-flex items-center gap-4 px-6 py-3 bg-white/10 rounded-2xl border border-white/20">
                            <div class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></div>
                            <p class="text-xs font-black uppercase tracking-widest">Available for new implementations</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- TOP 3 FIXES: TRANSFORMATIONS -->
    <section id="fixes" class="py-24 px-6 bg-slate-50/50 border-y border-slate-100">
        <div class="max-w-7xl mx-auto">
            <div class="text-center mb-24">
                <h2 class="text-4xl md:text-6xl font-black text-slate-900 tracking-tighter mb-8">The Transformation Strategy</h2>
                <p class="text-slate-500 font-medium max-w-2xl mx-auto text-xl">We don't just "fix websites." We implement these specific strategic shifts to move the needle.</p>
            </div>
            <% if (vm.top_3_issues.length > 0) { %>
            <div class="grid md:grid-cols-3 gap-8">
                <% vm.top_3_issues.slice(0, 3).forEach((fix, idx) => { %>
                <div class="bg-white p-12 rounded-[3.5rem] border border-slate-100 shadow-xl shadow-slate-200/50 hover:shadow-2xl transition-all hover:-translate-y-4 group">
                    <div class="w-20 h-20 bg-blue-50 text-blue-600 rounded-3xl flex items-center justify-center mb-12 shadow-lg group-hover:bg-blue-600 group-hover:text-white transition-all">
                        <% if (idx === 0) { %>
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" /></svg>
                        <% } else if (idx === 1) { %>
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4z" /></svg>
                        <% } else { %>
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" /></svg>
                        <% } %>
                    </div>
                    <h3 class="text-3xl font-black text-slate-900 mb-10 tracking-tight leading-none"><%= fix.title %></h3>
                    <div class="space-y-10">
                        <div>
                            <p class="text-[10px] font-black uppercase text-slate-400 mb-2 tracking-widest">Current Weakness</p>
                            <p class="text-sm text-slate-800 font-bold leading-relaxed"><%= fix.why_it_matters %></p>
                        </div>
                        <div class="pt-8 border-t border-slate-50">
                            <p class="text-[10px] font-black uppercase text-emerald-500 mb-2 tracking-widest">The "Wow" Outcome</p>
                            <% if (Array.isArray(fix.fix_steps)) { %>
                            <p class="text-md text-blue-900 font-black leading-tight italic"><%= fix.fix_steps[0] %></p>
                            <% } else { %>
                            <p class="text-md text-blue-900 font-black leading-tight italic"><%= fix.fix_steps %></p>
                            <% } %>
                        </div>
                    </div>
                </div>
                <% }); %>
            </div>
            <% } else { %>
            <div class="text-center py-20 text-slate-400">
                <p class="text-xl">Analysis in progress or insufficient data available.</p>
            </div>
            <% } %>
        </div>
    </section>

    <!-- THE 7-DAY DELIVERY -->
    <section class="py-32 px-6 max-w-5xl mx-auto">
        <div class="text-center mb-20">
            <h2 class="text-5xl md:text-7xl font-black mb-8 tracking-tighter">The 7-Day Sprint</h2>
            <p class="text-xl text-slate-500 font-medium">Fast, predictable, and professional. No endless meetings.</p>
        </div>
        <div class="grid gap-4">
            <% vm.seven_day_plan.forEach((step, i) => { %>
            <div class="flex items-center gap-10 p-10 bg-white border border-slate-100 rounded-[3rem] shadow-sm hover:shadow-2xl transition-all group">
                <span class="text-7xl font-black text-slate-50 group-hover:text-blue-100 transition-colors">0<%= i + 1 %></span>
                <span class="text-2xl text-slate-800 font-black tracking-tight"><%= step %></span>
            </div>
            <% }); %>
        </div>
    </section>

    <!-- FINAL CTA: THE INVITATION -->
    <section class="py-32 px-6 text-center bg-slate-950 text-white rounded-[5rem] mx-4 lg:mx-12 my-20 relative overflow-hidden">
        <div class="absolute top-0 left-0 w-full h-full opacity-5 pointer-events-none">
            <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none"><path d="M0,0 L100,0 L100,100 L0,100 Z" fill="url(#grad)"/><defs><linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" stop-color="#3b82f6"/><stop offset="100%" stop-color="#8b5cf6"/></linearGradient></defs></svg>
        </div>
        <div class="max-w-4xl mx-auto relative z-10">
            <h2 class="text-5xl md:text-8xl font-black mb-10 tracking-tighter">Stop losing <%= vm.hero.niche %> customers to <%= vm.hero.city %> competitors.</h2>
            <p class="text-2xl text-slate-400 font-medium mb-16 leading-relaxed">
                We've identified exactly what's wrong with <%= vm.hero.brand_or_domain %>. Answer 5 quick questions, and Max & Jacob will send you the complete 7-day transformation plan—customized for your business.
            </p>
            <a href="#form" class="inline-flex px-16 py-8 bg-blue-600 text-white rounded-[2.5rem] font-black text-3xl hover:bg-blue-700 shadow-[0_35px_60px_-15px_rgba(59,130,246,0.5)] transition-all transform hover:scale-105">
                Get My Free Plan
            </a>
            <div class="mt-16 pt-12 border-t border-white/10 flex flex-wrap justify-center gap-10 text-[10px] font-black uppercase tracking-[0.4em] text-slate-500">
                <span>Concept Review</span>
                <span>Accuracy Verified</span>
                <span>Max & Jacob Studio</span>
            </div>
        </div>
    </section>

    <!-- FORM SECTION -->
    <section class="py-24 px-6 max-w-4xl mx-auto" id="form">
        <div class="bg-white rounded-[3rem] p-12 shadow-2xl border border-slate-100">
            <div class="text-center mb-12">
                <h2 class="text-4xl font-black mb-4"><%= vm.form_config.headline %></h2>
                <p class="text-slate-500 text-lg"><%= vm.form_config.subheadline %></p>
            </div>

            <form action="<%= vm.form_config.action %>" method="<%= vm.form_config.method %>">
                <div class="grid md:grid-cols-2 gap-6 mb-6">
                    <div class="flex flex-col">
                        <label class="mb-2 text-slate-700 font-semibold text-sm">Name *</label>
                        <input type="text" name="name" required placeholder="Your name" class="px-4 py-3 border border-slate-200 rounded-2xl focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition-all">
                    </div>

                    <div class="flex flex-col">
                        <label class="mb-2 text-slate-700 font-semibold text-sm">Email *</label>
                        <input type="email" name="email" required placeholder="your@email.com" class="px-4 py-3 border border-slate-200 rounded-2xl focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition-all">
                    </div>

                    <div class="flex flex-col">
                        <label class="mb-2 text-slate-700 font-semibold text-sm">Website</label>
                        <input type="text" name="website" value="<%= vm.form_config.prefill.website %>" placeholder="www.yoursite.com" class="px-4 py-3 border border-slate-200 rounded-2xl focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition-all">
                    </div>

                    <div class="flex flex-col">
                        <label class="mb-2 text-slate-700 font-semibold text-sm">Budget Range</label>
                        <select name="budget_range" class="px-4 py-3 border border-slate-200 rounded-2xl focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition-all">
                            <option value="">Select budget</option>
                            <option value="5k-10k">$5,000 – $10,000</option>
                            <option value="10k-20k">$10,000 – $20,000</option>
                            <option value="20k+">$20,000+</option>
                        </select>
                    </div>

                    <div class="flex flex-col md:col-span-2">
                        <label class="mb-2 text-slate-700 font-semibold text-sm">Are you the decision maker?</label>
                        <select name="decision_maker" class="px-4 py-3 border border-slate-200 rounded-2xl focus:outline-none focus:border-blue-600 focus:ring-4 focus:ring-blue-100 transition-all">
                            <option value="">Select</option>
                            <option value="yes">Yes</option>
                            <option value="no">No</option>
                        </select>
                    </div>
                </div>

                <button type="submit" class="w-full px-12 py-5 bg-blue-600 text-white rounded-[2rem] font-black text-xl hover:bg-blue-700 shadow-xl shadow-blue-200 transition-all hover:scale-[1.02] active:scale-95">
                    <%= vm.form_config.cta_button_text %>
                </button>

                <p class="text-center text-slate-400 text-sm mt-6">
                    <%= vm.form_config.disclaimer %>
                </p>
            </form>
        </div>
    </section>

    <!-- FOOTER -->
    <footer class="py-24 border-t border-slate-100 bg-white">
        <div class="max-w-7xl mx-auto px-6 flex flex-col md:flex-row justify-between items-center gap-16">
            <div class="text-center md:text-left">
                <div class="font-black text-4xl tracking-tighter text-blue-600 mb-4">Max & Jacob</div>
                <p class="text-md text-slate-500 font-bold max-w-xs">Building trust engines for elite local service providers.</p>
            </div>
            <div class="text-center md:text-right">
                <p class="text-xs text-slate-400 font-black uppercase tracking-[0.3em] mb-4 italic">No guarantees, just clarity.</p>
                <div class="flex justify-center md:justify-end gap-10 text-xs font-black uppercase text-slate-400 tracking-widest">
                    <a href="https://maxandjacob.com/privacy" class="hover:text-blue-600">Privacy</a>
                    <a href="#" class="hover:text-blue-600">Terms</a>
                    <a href="mailto:hello@maxandjacob.com" class="hover:text-blue-600">Contact</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- DEBUG SECTION -->
    <details class="max-w-7xl mx-auto px-6 mb-12 bg-slate-900 border border-slate-700 rounded-2xl p-6">
        <summary class="font-bold text-blue-400 cursor-pointer text-sm">🔧 Debug Info (Internal)</summary>
        <div class="mt-4 text-slate-300 text-sm space-y-2">
            <div><strong>Job ID:</strong> <%= vm.debug.job_id %></div>
            <div><strong>URL:</strong> <%= vm.debug.input_url %></div>
            <div><strong>Status:</strong> <%= vm.debug.status %></div>
            <div class="mt-4"><strong>Coverage:</strong></div>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-2 mt-2">
                <div class="bg-slate-800 p-2 rounded text-xs">Screenshots: <%= vm.debug.coverage.screenshots ? '✅' : '❌' %></div>
                <div class="bg-slate-800 p-2 rounded text-xs">UX Audit: <%= vm.debug.coverage.ux_audit ? '✅' : '❌' %></div>
                <div class="bg-slate-800 p-2 rounded text-xs">SEO Audit: <%= vm.debug.coverage.seo_audit ? '✅' : '❌' %></div>
                <div class="bg-slate-800 p-2 rounded text-xs">Offer Copy: <%= vm.debug.coverage.offer_copy ? '✅' : '❌' %></div>
            </div>
        </div>
    </details>

    <script>
        // Zoom modal functionality
        function openZoom(imageUrl) {
            const modal = document.getElementById('zoomModal');
            const img = document.getElementById('zoomImage');
            img.src = imageUrl;
            modal.classList.remove('hidden');
        }

        document.getElementById('zoomModal').addEventListener('click', function() {
            this.classList.add('hidden');
        });

        document.getElementById('zoomImage').addEventListener('click', function(e) {
            e.stopPropagation();
        });

        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
    </script>
</body>
</html>
