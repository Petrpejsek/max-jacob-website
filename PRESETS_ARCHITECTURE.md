# Niche Presets – Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                          ADMIN UI                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Audit Job Detail Page                                   │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐         │  │
│  │  │ LLM Settings│  │  Presets   │  │ Back/Logout│         │  │
│  │  └────────────┘  └────────────┘  └────────────┘         │  │
│  │         ▼               ▼                                │  │
│  │    LLM Modal       Presets Modal                         │  │
│  │                    ┌───────────────┐                     │  │
│  │                    │ Tab: List     │                     │  │
│  │                    │  - View       │                     │  │
│  │                    │  - Edit       │                     │  │
│  │                    │  - Delete     │                     │  │
│  │                    ├───────────────┤                     │  │
│  │                    │ Tab: Form     │                     │  │
│  │                    │  - Create     │                     │  │
│  │                    │  - Edit       │                     │  │
│  │                    │  - Upload     │                     │  │
│  │                    └───────────────┘                     │  │
│  │                                                           │  │
│  │  ┌────────────────────────────────────────────────────┐ │  │
│  │  │ A) Input Section                                   │ │  │
│  │  │  - URL                                             │ │  │
│  │  │  - Niche                                           │ │  │
│  │  │  - City                                            │ │  │
│  │  │  - **Niche Preset** ◄── NEW DROPDOWN              │ │  │
│  │  │  - Company Name                                    │ │  │
│  │  │  - Logo URL                                        │ │  │
│  │  │                                                     │ │  │
│  │  │  ┌─────────────────────────────────────────────┐  │ │  │
│  │  │  │ Preset Preview (conditional)                │  │ │  │
│  │  │  │  - Image                                    │  │ │  │
│  │  │  │  - Headline, CTA, Bullets                   │  │ │  │
│  │  │  └─────────────────────────────────────────────┘  │ │  │
│  │  └────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       BACKEND API                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  /api/presets                                            │  │
│  │   - GET /                   (list all)                   │  │
│  │   - GET /:id                (get one)                    │  │
│  │   - POST /                  (create + upload)            │  │
│  │   - PUT /:id                (update + upload)            │  │
│  │   - DELETE /:id             (delete + cleanup)           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Multer Middleware (File Upload)                        │  │
│  │   - Max 5MB                                              │  │
│  │   - PNG/JPG/JPEG/WEBP only                              │  │
│  │   - Storage: public/presets/{slug}-{timestamp}.ext      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Database Layer (db.js)                                  │  │
│  │   - createNichePreset()                                  │  │
│  │   - getAllNichePresets()                                 │  │
│  │   - getNichePresetById()                                 │  │
│  │   - updateNichePreset()                                  │  │
│  │   - deleteNichePreset()                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                         DATABASE                                │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  niche_presets                                           │  │
│  │   - id (PK)                                              │  │
│  │   - slug (UNIQUE)                                        │  │
│  │   - display_name                                         │  │
│  │   - concept_image_url                                    │  │
│  │   - default_headline                                     │  │
│  │   - default_primary_cta                                  │  │
│  │   - default_secondary_cta                                │  │
│  │   - default_city                                         │  │
│  │   - default_bullets_json                                 │  │
│  │   - created_at, updated_at                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              │ FK                               │
│                              ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  audit_jobs                                              │  │
│  │   - id (PK)                                              │  │
│  │   - preset_id (FK) ◄── NEW FIELD                        │  │
│  │   - input_url                                            │  │
│  │   - niche                                                │  │
│  │   - city                                                 │  │
│  │   - ... (existing fields)                                │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow: Audit Pipeline with Preset

```
┌─────────────────────────────────────────────────────────────────┐
│  1. ADMIN CREATES PRESET                                        │
│     ┌──────────────────────────────────────────┐               │
│     │ Admin UI → POST /api/presets             │               │
│     │  - FormData with image file              │               │
│     │  - slug, display_name, CTA, bullets      │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ Multer saves image to:                   │               │
│     │  public/presets/{slug}-{timestamp}.ext   │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ DB: INSERT INTO niche_presets            │               │
│     │  concept_image_url = "public/presets/..." │              │
│     └──────────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  2. ADMIN SELECTS PRESET IN AUDIT JOB                          │
│     ┌──────────────────────────────────────────┐               │
│     │ Admin UI: Dropdown "Niche Preset"       │               │
│     │  → Select "Plumbing"                     │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ JavaScript: loadPresets()                │               │
│     │  → GET /api/presets                      │               │
│     │  → Populate dropdown                     │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ On change: showPresetPreview(presetId)  │               │
│     │  → Display image + texty                 │               │
│     └──────────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  3. ADMIN RUNS AUDIT PIPELINE                                   │
│     ┌──────────────────────────────────────────┐               │
│     │ Admin clicks "Process"                   │               │
│     │  → POST /admin/audits/:id/process        │               │
│     │     body: { preset_id: X, ... }          │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ DB: UPDATE audit_jobs                    │               │
│     │   SET preset_id = X                      │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ auditPipeline.processAuditJob(jobId)    │               │
│     └──────────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  4. PIPELINE RUNS WITH PRESET                                   │
│     ┌──────────────────────────────────────────┐               │
│     │ loadJob(jobId)                           │               │
│     │  → job.preset_id = X                     │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ if (job.preset_id) {                     │               │
│     │   preset = getNichePresetById(X)         │               │
│     │ }                                        │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ Scrape website (as usual)                │               │
│     │  → screenshots                           │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ Run LLM evaluators (as usual)            │               │
│     │  → miniAudit (issues, plan, suggestions) │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ generateEmailHtml(job, miniAudit,        │               │
│     │                   screenshots, preset)   │               │
│     │  ▼                                       │               │
│     │  if (preset && preset.concept_image_url) │               │
│     │    imageUrl = preset.concept_image_url   │               │
│     │  else                                    │               │
│     │    imageUrl = screenshots.above_fold     │               │
│     │                                          │               │
│     │  CTA = preset.default_primary_cta        │               │
│     │     || miniAudit.copy_suggestions[0]     │               │
│     │                                          │               │
│     │  Disclaimer:                             │               │
│     │   "This is a concept example for {niche} │               │
│     │    businesses in {city}, not your        │               │
│     │    current website..."                   │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ generateConceptPreview(job, miniAudit,   │               │
│     │                        preset)            │               │
│     │  ▼                                       │               │
│     │  headline = preset.default_headline      │               │
│     │            .replace('{city}', job.city)  │               │
│     │            .replace('{niche}', job.niche)│               │
│     │                                          │               │
│     │  cta = preset.default_primary_cta        │               │
│     │  secondary_cta = preset.default_secondary│               │
│     │  bullets = preset.default_bullets_json   │               │
│     │  concept_image = preset.concept_image_url│               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ generatePublicPageJson(job, miniAudit,   │               │
│     │                        conceptPreview,    │               │
│     │                        screenshots, preset│               │
│     │  ▼                                       │               │
│     │  Uses conceptPreview (with preset data)  │               │
│     │  form.cta = preset.default_primary_cta   │               │
│     │  disclaimer = "This is a concept..."     │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ DB: UPDATE audit_jobs                    │               │
│     │   SET email_html = ...,                  │               │
│     │       public_page_json = ...,            │               │
│     │       status = 'ready'                   │               │
│     └──────────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  5. OUTPUTS (EMAIL & PUBLIC PAGE)                               │
│     ┌──────────────────────────────────────────┐               │
│     │ Email HTML                               │               │
│     │  ┌────────────────────────────────────┐  │               │
│     │  │ <img src="/public/presets/plumb... │  │               │
│     │  │ <p>Concept preview for your        │  │               │
│     │  │    industry</p>                    │  │               │
│     │  │ <a>Get a Quote</a>                 │  │               │
│     │  │ <p>This is a concept example for   │  │               │
│     │  │    plumbing businesses in Miami... │  │               │
│     │  └────────────────────────────────────┘  │               │
│     └──────────────────────────────────────────┘               │
│                       ▼                                         │
│     ┌──────────────────────────────────────────┐               │
│     │ Public Page (EJS)                        │               │
│     │  ┌────────────────────────────────────┐  │               │
│     │  │ Concept Preview Section:           │  │               │
│     │  │  <img src="/public/presets/plumb...│  │               │
│     │  │  <h2>More calls from your Miami    │  │               │
│     │  │      plumbing website</h2>         │  │               │
│     │  │  Primary CTA: Get a Quote          │  │               │
│     │  │  Secondary CTA: Call Now           │  │               │
│     │  │  Bullets:                          │  │               │
│     │  │   - Fast 24/7 response             │  │               │
│     │  │   - Licensed & insured             │  │               │
│     │  │   - Free estimates                 │  │               │
│     │  │  <small>This is a concept example  │  │               │
│     │  │    for plumbing businesses in      │  │               │
│     │  │    Miami, not your current         │  │               │
│     │  │    website.</small>                │  │               │
│     │  └────────────────────────────────────┘  │               │
│     └──────────────────────────────────────────┘               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ File Storage Structure

```
Max&Jacob/
├── public/
│   └── presets/                      ◄── NEW
│       ├── plumbing-1234567890.png   ◄── Uploaded concept images
│       ├── roofing-1234567891.jpg
│       ├── hvac-1234567892.webp
│       └── ...
│
├── server/
│   ├── db.js                         ◄── Modified (+ CRUD for presets)
│   ├── server.js                     ◄── Modified (+ preset router)
│   ├── routes/
│   │   ├── admin.js                  ◄── Modified (+ preset_id support)
│   │   ├── presets.js                ◄── NEW (preset API routes)
│   │   └── ...
│   ├── services/
│   │   └── auditPipeline.js          ◄── Modified (+ preset integration)
│   └── views/
│       ├── admin-audit-detail.ejs    ◄── Modified (+ Presets modal)
│       └── audit-public.ejs          ◄── Modified (+ preset concept preview)
│
├── data.db                           ◄── Modified (+ niche_presets table)
├── NICHE_PRESETS_GUIDE.md            ◄── NEW
├── PRESETS_TESTING_CHECKLIST.md      ◄── NEW
├── IMPLEMENTATION_COMPLETE.md        ◄── NEW
└── PRESETS_ARCHITECTURE.md           ◄── NEW (this file)
```

---

## 🔗 Component Relationships

```
┌───────────────────────────────────────────────────────────────────┐
│                      PRESET LIFECYCLE                             │
└───────────────────────────────────────────────────────────────────┘

  CREATE
    ↓
┌─────────────────┐
│ Admin UI        │ → POST /api/presets
│ (Presets Modal) │   + multipart/form-data
└─────────────────┘         │
                            ↓
                  ┌──────────────────┐
                  │ Multer Middleware│ → Save image
                  └──────────────────┘
                            │
                            ↓
                  ┌──────────────────┐
                  │ db.createPreset()│ → INSERT
                  └──────────────────┘
                            │
                            ↓
                  ┌──────────────────┐
                  │ niche_presets DB │
                  └──────────────────┘

  USE
    ↓
┌─────────────────┐
│ Audit Job Form  │ → Select preset from dropdown
│ (Input Section) │   → job.preset_id = X
└─────────────────┘         │
                            ↓
                  ┌──────────────────┐
                  │ Pipeline runs    │ → getNichePresetById(X)
                  └──────────────────┘
                            │
                            ↓
                  ┌──────────────────┐
                  │ Email + Public   │ → Use preset data
                  │ Page Generators  │   (image, CTA, bullets)
                  └──────────────────┘

  UPDATE
    ↓
┌─────────────────┐
│ Admin UI        │ → PUT /api/presets/:id
│ (Edit Form)     │   + multipart/form-data
└─────────────────┘         │
                            ↓
                  ┌──────────────────┐
                  │ Delete old image │ (if new uploaded)
                  └──────────────────┘
                            │
                            ↓
                  ┌──────────────────┐
                  │ Save new image   │
                  └──────────────────┘
                            │
                            ↓
                  ┌──────────────────┐
                  │ db.updatePreset()│ → UPDATE
                  └──────────────────┘

  DELETE
    ↓
┌─────────────────┐
│ Admin UI        │ → DELETE /api/presets/:id
│ (Presets List)  │
└─────────────────┘         │
                            ↓
                  ┌──────────────────┐
                  │ Get preset data  │
                  └──────────────────┘
                            │
                            ↓
                  ┌──────────────────┐
                  │ db.deletePreset()│ → DELETE from DB
                  └──────────────────┘
                            │
                            ↓
                  ┌──────────────────┐
                  │ Delete image file│ → unlink()
                  └──────────────────┘
```

---

## 🎯 Key Design Decisions

### 1. **Preset as "Truth Source"**
- Preset defaults jsou primární zdroj dat
- Job nemůže overridovat (v MVP)
- LLM suggestions jsou sekundární (neoverrideují preset)

**Důvod**: Konzistence a rychlost. Admin má kontrolu nad messaging per niche.

### 2. **Concept Image vs. Screenshot**
- Preset má concept image → generický mockup pro niche
- Screenshot je fallback (pokud preset neexistuje nebo nemá image)

**Důvod**: Eliminuje problém fake personalizovaných mockupů.

### 3. **Disclaimers Always Present**
- Email: "Concept preview for your industry"
- Public page: "This is a concept example for {niche} businesses in {city}, not your current website."

**Důvod**: Compliance a jasná komunikace.

### 4. **Upload to public/presets/**
- Obrázky jsou veřejně přístupné (ne v uploads/)
- Unikátní názvy: `{slug}-{timestamp}.ext`

**Důvod**: Jednoduchost a rychlost. Není potřeba autorizace pro obrázky.

### 5. **Multer for File Upload**
- Standard Node.js middleware
- Validace (type, size) na backendu

**Důvod**: Spolehlivost a bezpečnost.

---

**Dokument vytvořen**: 2026-01-15  
**Verze**: 1.0.0

